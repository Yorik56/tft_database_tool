{% extends 'base.html.twig' %}

{% block title %}New Item{% endblock %}

{% block body %}
    <h1>Create new Item</h1>
    <div class="container">
        {{ include('item/_form.html.twig') }}
        <a class="btn btn-primary" href="{{ path('app_item_index') }}">back to list</a>
    </div>
{% endblock %}
