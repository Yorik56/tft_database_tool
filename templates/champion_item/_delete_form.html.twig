<form method="post" action="{{ path('app_champion_item_delete', {'id': champion_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ champion_item.id) }}">
    <button class="btn">Delete</button>
</form>
