{% extends 'base.html.twig' %}

{% block title %}ChampionItem{% endblock %}

{% block body %}
    <h1>ChampionItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ champion_item.id }}</td>
            </tr>
            <tr>
                <th>Rank</th>
                <td>{{ champion_item.rank }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_champion_item_index') }}">back to list</a>

    <a href="{{ path('app_champion_item_edit', {'id': champion_item.id}) }}">edit</a>

    {{ include('champion_item/_delete_form.html.twig') }}
{% endblock %}
